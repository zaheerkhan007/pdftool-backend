Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-environ==0.11.2
psycopg2-binary==2.9.9
gunicorn==22.0.0
whitenoise==6.7.0

# PDF / file processing
pypdf==4.2.0
pikepdf==9.0.0
Pillow==10.3.0
pdf2image==1.17.0
reportlab==4.2.0
PyMuPDF>=1.28.0          # PDF → JPG/PPTX/Markdown rendering (abi3 wheel, no system deps)
pdf2docx>=0.5.13         # PDF → Word (pure pip: PyMuPDF + python-docx + opencv-headless)
python-pptx>=0.6.23      # PDF → PowerPoint (one page-image per slide)
openpyxl>=3.1.2          # PDF → Excel (write extracted tables)
pdfplumber>=0.11.0       # PDF → Excel (table/text extraction)
ocrmypdf>=16.0.0         # OCR PDF (needs tesseract-ocr + ghostscript system pkgs)
anthropic>=0.40.0        # Summarize / Translate (needs ANTHROPIC_API_KEY; degrades to 503 without)

# Async jobs (optional but wired up)
celery==5.4.0
redis==5.0.4

# Optional heavy tools (uncomment when you add these features)
# ocrmypdf==16.4.0        # OCR  (needs tesseract + ghostscript system pkgs)
# pdfplumber==0.11.0      # table extraction
